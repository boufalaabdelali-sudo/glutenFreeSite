PORT=3000
MONGODB_URI=mongodb://127.0.0.1:27017/sans_gluten_express

# Secret pour signer les JWT (obligatoire en production)
JWT_SECRET=votre_cle_secrete_longue_et_aleatoire

# Utilise par npm run seed:admin (creation du premier compte si absent)
ADMIN_SEED_USERNAME=admin
ADMIN_SEED_PASSWORD=changeme
ADMIN_SEED_ROLE=owner

# Optionnel — fichier JSON de marque client (defaut: config/branding.json)
# BRANDING_PATH=config/branding.json
